# Copy to .env at the repo root (gitignored) to enable the dev preview menu.
# See docs/TESTING.md's "Dev preview menu" section.
#
# 1 = show the "Dev: preview state" section in the popup menu.
# 0, any other value, or deleting the file entirely = off (normal behavior).
CLAUDEWATCH_DEV=0
